# 🤖 بوت تيليجرام للعروض التلقائي

بوت يسحب العروض تلقائياً من مصادر متعددة وينشرها في قناة تيليجرام.

## ⚡ المميزات

- ✅ سحب العروض تلقائياً كل ساعة
- ✅ نشر في قناة تيليجرام
- ✅ تجنب تكرار العروض
- ✅ تنسيق جميل للرسائل
- ✅ إحصائيات وأوامر إدارة

## 🚀 التثبيت والتشغيل

### 1. تثبيت المتطلبات

```bash
pip install -r requirements.txt
```

### 2. إعداد البوت

1. اذهب لـ [@BotFather](https://t.me/BotFather) في تيليجرام
2. أرسل `/newbot` واتبع التعليمات
3. انسخ التوكن

### 3. إعداد القناة

1. أنشئ قناة جديدة في تيليجرام
2. أضف البوت كمشرف في القناة
3. امنحه صلاحية "Post Messages"

### 4. تعديل الإعدادات

افتح ملف `config.py` وعدّل:

```python
BOT_TOKEN = "ضع_التوكن_هنا"
CHANNEL_ID = "@اسم_قناتك"
ADMIN_IDS = [معرفك_الرقمي]  # استخدم @userinfobot لمعرفته
```

### 5. تشغيل البوت

```bash
python bot.py
```

## 📱 الأوامر

| الأمر | الوصف |
|-------|-------|
| `/start` | بدء البوت |
| `/latest` | آخر العروض |
| `/stats` | الإحصائيات |
| `/refresh` | تحديث يدوي (للمشرفين) |
| `/help` | المساعدة |

## 📂 هيكل المشروع

```
telegram-offers-bot/
├── bot.py              # البوت الرئيسي
├── config.py           # الإعدادات
├── database.py         # قاعدة البيانات
├── requirements.txt    # المكتبات
└── scrapers/
    ├── __init__.py
    └── rss_scraper.py  # سحب من RSS
```

## 🔧 إضافة مصادر جديدة

في ملف `config.py`:

```python
RSS_FEEDS = [
    {
        "name": "اسم المصدر",
        "url": "رابط RSS Feed",
        "category": "التصنيف"
    },
    # أضف المزيد هنا...
]
```

## 📍 للحصول على معرفك (User ID)

1. ابحث عن @userinfobot في تيليجرام
2. أرسل `/start`
3. سيظهر لك معرفك الرقمي

## ⚠️ ملاحظات

- البوت يحتاج يكون مشرف في القناة للنشر
- تأكد من صلاحيات النشر
- العروض تُحفظ لتجنب التكرار

---

صنع بـ ❤️ للمجتمع العربي
